```markdown
# Flask todo-app

A simple todo list application built with Flask.

## Setup

First you have to clone it. then..

>> cd /path

1. python -m venv venv
source venv/bin/activate

2. pip install -r requirements.txt

3. python app.py
